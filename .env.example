# Server configuration
PORT=3000

# LLM configuration (OpenRouter)
OPENAI_API_KEY=replace-with-openrouter-api-key

# Retrieval & embeddings configuration
# MiniLM (384-dim) embeddings are computed server-side via Chroma's DefaultEmbeddingFunction.
CHROMA_URL=http://localhost:8000
CHROMA_COLLECTION=ground-truth-minilm
CHROMA_BATCH_SIZE=64
