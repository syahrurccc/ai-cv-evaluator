# Server configuration
PORT=3000

# LLM configuration
LLM_API_KEY=replace-with-llm-api-key
LLM_BASE_URL=
LLM_MODEL=

# Retrieval & embeddings configuration
# OllamaEmbeddingFunction will embed documents/queries with the specified local model.
OLLAMA_EMBED_URL=
OLLAMA_EMBED_MODEL=
# Chroma will derive the vector dimension from the Ollama model; use a fresh collection per model change.
CHROMA_URL=
CHROMA_COLLECTION=
CHROMA_BATCH_SIZE=
